spring:
  application:
    name: reaction-service-test
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

server:
  port: -1
